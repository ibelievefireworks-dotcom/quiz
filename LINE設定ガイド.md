# 家族LINEグループへ「各単元の結果」を自動投稿する設定ガイド（詳細版）

その日にやった分（4択・書く言う）を、**1日3回（18時・20時・23時）にまとめて家族LINEグループへ自動で届く**ようにします（1日最大3通＝月90通ほどで無料枠に余裕）。
作業はパソコン推奨。最後に出る **/exec のURL** を私に教えてください。

> 画面の文言はLINE/Googleの更新で多少変わることがあります。太字の用語で検索すれば見つかります。

---

## 全体像（3つの登場人物）
1. **クイズ**（GitHub Pages）… 結果テキストを送る
2. **GAS（Google Apps Script）**… 受け取ってLINEへ中継する“係”。トークンはここだけに置くので安全
3. **LINE Bot（Messaging API）**… 家族グループへ投稿する

順番は **B(GAS)を仮作成 → A(LINE) → AとBを接続 → グループ登録 → URLを私に連絡** が一番スムーズです。

---

## STEP A. LINEのBotを作る

1. パソコンで **https://developers.line.biz/console/** を開く。
2. 右上などの **「ログイン」→「LINEアカウントでログイン」**。スマホのLINEで承認（初回は開発者登録：名前・メールを入力して同意）。
3. **「プロバイダー」**を作る：**「Create」/「新規プロバイダー作成」**→ 名前（例：`莉映クイズ`）→ **作成**。
   - ※プロバイダー＝入れ物の名前。何でもOK。
4. そのプロバイダーの画面で **「Messaging APIチャネルを作成」**（または「新規チャネル作成」→種類で **Messaging API** を選ぶ）。
5. 入力フォーム：
   - **チャネル名**：例 `莉映の勉強Bot`（あとで変更可）
   - **チャネル説明**：例 `クイズ結果の通知`
   - **大業種／小業種**：適当でOK（例：個人／その他）
   - **メールアドレス**：自分のメール
   - 規約に同意 → **作成**。
6. 作成後、そのチャネルを開き、上のタブで **「Messaging API設定」** を開く。
7. 一番下の **「チャネルアクセストークン（長期）」** の **「発行」** を押す → 出てきた**長い文字列をコピー**してメモ（あとでGASに貼る）。
   - ※この画面の上の方に **QRコード**（Bot本体）もあります。STEP Dで使います。

> ⚠️ この時点ではまだWebhook URLは入れません（GASのURLができてから STEP C で入れます）。

---

## STEP B. Google Apps Script（中継係）を作る

8. **https://script.google.com/** を開き **「新しいプロジェクト」**。
9. 最初から入っている `function myFunction(){}` を**全部消す**。
10. リポジトリの **`gas-line-bot.gs` の中身を全部貼り付け**。
11. 先頭の行
    ```
    var CHANNEL_ACCESS_TOKEN = "ここに_チャネルアクセストークン_を貼る";
    ```
    の `"..."` の中を、**STEP A-7でコピーしたトークン**に置き換える → 💾 **保存**（Ctrl/Cmd+S）。
12. 右上 **「デプロイ」→「新しいデプロイ」**。
    - **「種類の選択」（歯車⚙）→「ウェブアプリ」** を選ぶ。
    - **次のユーザーとして実行**：**自分**
    - **アクセスできるユーザー**：**全員**  ←ここ重要（クイズから匿名で送るため）
    - **「デプロイ」**。
13. **権限の承認**が出ます：
    - 「アクセスを承認」→ 自分のGoogleアカウントを選択
    - 「このアプリはGoogleで確認されていません」→ **「詳細」→「（プロジェクト名）に移動（安全ではない）」** をクリック → **「許可」**。
    - ※自分で作ったアプリなので問題ありません。
14. 完了画面の **「ウェブアプリ」のURL（末尾が `/exec`）** を**コピー**してメモ。
15. **毎晩のまとめ送信を予約する（一度だけ）**：
    - GASエディタ上部の関数名のプルダウンで **`setupDailyTrigger`** を選び **▶実行** → 初回は権限承認（STEP B-13と同様に「許可」）。
    - これで **1日3回（18時・20時・23時）に、たまっている分を自動送信** が予約されます。
16. **時刻を日本時間に合わせる**：左メニューの **⚙「プロジェクトの設定」→「タイムゾーン」→「(GMT+09:00) 日本標準時 / Tokyo」** を選択。

---

## STEP C. LINEとGASをつなぐ

15. STEP Aの **「Messaging API設定」** に戻る。
16. **「Webhook URL」** の欄に、**STEP B-14の /exec のURL** を貼って **「更新」**。
17. すぐ下の **「Webhookの利用」** を **オン**。
18. （静かにするため）**「応答設定」/「LINE公式アカウント機能」** を開く（→ LINE Official Account Manager に飛びます）。
    - **応答モード**：**Bot**（またはチャット＝オフ）
    - **あいさつメッセージ**：オフ（任意）
    - **応答メッセージ**：**オフ**
    - **Webhook**：**オン**
    - **「グループ・複数人トークへの参加を許可」**：**オン**  ←これがオフだとBotをグループに入れられません

---

## STEP D. 家族グループに入れて登録する

19. STEP A-7の画面上部の **QRコード**をスマホLINEで読み取り、Botを **友だち追加**。
20. **家族のLINEグループ**を開き、**メンバー追加（招待）**で、いま友だち追加したBot（公式アカウント）を**グループに招待**。
21. そのグループで **何かひとことメッセージを送る**（例：「テスト」）。
22. Botが **「✅ このグループを登録しました…」** と返信すれば成功（グループIDが自動保存されました）。
    - 返信が来ない場合は STEP C-16/17（Webhook URL・利用オン）と C-18（Webhookオン・グループ参加許可）を再確認。

---

## STEP E. クイズ側をオンにする（私の作業）

23. **STEP B-14の /exec のURLを私に教えてください。**
    → クイズの `report.js` に設定して公開します。これで、その日にやった分が**毎晩まとめて1通**届きます 🎉

---

## どう届く？
- お子さんがやった単元はGASに“ためられ”、**1日3回（18時・20時・23時）に**、その時点でたまっている分がまとめて届きます。
- 例：
  ```
  📚 学習まとめ（4単元 ・ 合格3）
  ――――――――――
  📘 地理：日本の地形・気候  83% ✅
  🧪 理科：化学反応式  90% ✅
  ✏️ 漢字の書き取り  75% 💪
  📖 国語：枕草子  80% ✅
  ――――――――――
  この調子でいこう！ #莉映の期末対策
  ```
- 新しくやった分が無い回は送りません（無料枠の節約）。
- 各回は「前回の送信以降にやった分」をまとめます（23時の次は翌日18時）。

## 動作テスト
- `testPush` を ▶実行 … 接続テスト（今すぐ1通届く）。先にSTEP Dの登録が必要。
- `testDigestNow` を ▶実行 … いまためている分を、夜を待たず**今すぐまとめ送信**して確認。

## 注意（無料枠）
- LINE無料枠は **月200通** 目安。この方式は **1日最大3通（月90通ほど）** なので余裕で収まります。
- 送信時刻（18/20/23時）や回数の変更、「合格したときだけ強調」なども調整できます。

## つまずいたら
- どのSTEPの何番で、画面に何が出ているかを教えてください。その場で解決します。
