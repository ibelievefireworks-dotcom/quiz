# 家族LINEグループへ「各単元の結果」を自動投稿する設定ガイド

各単元（4択クイズ・書く言う）を終えると、結果が**家族LINEグループへ自動で届く**ようにします。
無料です。初期設定だけ一度（約15分）お願いします。最後に出てくる **/exec のURL** を私に教えてください。

---

## 全体像
クイズ（GitHub Pages）→ Google Apps Script（GAS）→ LINE Messaging API → 家族グループ

- GAS が「結果を受け取ってLINEへ送る係」。トークンはGAS内にだけ置くので安全（公開ページには出ません）。
- グループIDは、Botをグループに入れて一言送ると**自動で登録**されます。

---

## 手順

### A. LINEのBot（Messaging API）を作る
1. パソコンで **https://developers.line.biz/console/** を開き、自分のLINEアカウントでログイン。
2. 「新規プロバイダー作成」→ 名前（例：莉映クイズ）→ 作成。
3. そのプロバイダーで「新規チャネル作成」→ **Messaging API** を選択 → 必要事項を入力して作成。
4. 作ったチャネルの **「Messaging API設定」タブ** を開く。
5. 一番下の **「チャネルアクセストークン（長期）」** を「発行」→ 表示された長い文字列を**コピー**。

### B. Google Apps Script を用意する
6. **https://script.google.com/** を開き「新しいプロジェクト」。
7. 既定のコードを消し、**`gas-line-bot.gs` の中身を全部貼り付け**。
8. 先頭の `CHANNEL_ACCESS_TOKEN = "..."` の `...` を、**Aでコピーしたトークン**に置き換える → 保存（💾）。
9. 右上 **「デプロイ」→「新しいデプロイ」**。
   - 種類（歯車）→ **ウェブアプリ**
   - 「次のユーザーとして実行」→ **自分**
   - 「アクセスできるユーザー」→ **全員**
   - 「デプロイ」→ 権限の承認（自分のGoogleアカウントで許可）。
10. 表示される **ウェブアプリのURL（末尾が /exec）** を**コピー**。

### C. BotとGASをつなぐ
11. Aの「Messaging API設定」に戻り、**Webhook URL** に **/exec のURL** を貼って「更新」→ **「Webhookの利用」をオン**。
12. 同じ画面で **「応答メッセージ」をオフ**（自動の定型返信を止める）にしておくと静かです。

### D. グループに入れて登録
13. 「Messaging API設定」の上部にある **QRコード**から、Botを**友だち追加**。
14. **家族のLINEグループにそのBotを招待**（メンバー追加）。
15. グループで**何かひとことメッセージを送る** → Botが「✅ このグループを登録しました」と返信すればOK（グループIDが自動保存されます）。

### E. クイズ側をオンにする（私の作業）
16. **Bで出た /exec のURLを私に教えてください。**
    → クイズの `report.js` の `REPORT_URL` にそのURLを設定して公開します。
    これで、各単元を終えるたびに結果がグループへ自動投稿されます 🎉

---

## できること（オン後）
- 4択クイズ・書く言うを**1単元やり終えるごと**に、科目・単元名・正答率・合否（80%）がグループへ自動で届きます。
- 例：`📘 地理：日本の地形・気候 / 4択クイズ 12問 → 10問正解（83%）/ 🎉 合格ライン80%クリア！`

## 補足
- 動作テスト：GASエディタで関数 `testPush` を実行すると、グループにテスト送信されます。
- 投稿が多すぎると感じたら、「満点や合格のときだけ送る」等に調整できます（言ってください）。
- セキュリティ：/exec のURLは家族内だけで共有してください（URLを知っている人が送信できるため）。必要なら合言葉チェックも追加できます。
